{
    "tasks": [
        {
            "label": "Preassembling",
            "type": "shell",
            "command": "${config:activeProject}/sys/preassembling.exe",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": false
            }
        },

        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file",
            "command": "C:\\msys64\\ucrt64\\bin\\g++.exe",
            "args": [
                "-fdiagnostics-color=always",
				"-std=c++14",
                "-O0",
                "-g3",
				"-D${config:sdds_noDebug}",
                "${workspaceFolder}/*.cpp",
				"${workspaceFolder}/${config:activeProject}/sys/SDDS/src/*.cpp",
				"-I${workspaceFolder}/${config:activeProject}",
				"-I${workspaceFolder}/${config:activeProject}/sys/SDDS/src",
                "-I${workspaceFolder}/${config:activeProject}/sys/SDDS/test",
				"-I${workspaceFolder}/${config:activeProject}/sys/vbusSpike/src",
                "-I${workspaceFolder}/${config:activeProject}/sys/vbusSpike/debug",
				"-I${workspaceFolder}/${config:activeProject}/sys/textDisplaySpike/src",
				"-I${workspaceFolder}/${config:activeProject}/sys/src",
				"-I${workspaceFolder}/${config:activeProject}/sys",
				"-o",
                "${workspaceFolder}\\app.exe",
                "-lws2_32",
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
			"dependsOn": [
                "Preassembling"
            ],

            "detail": "Task generated by Debugger."
        },
    ],
    "version": "2.0.0"
}